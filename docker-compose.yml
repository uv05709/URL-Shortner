services:
  db:
    image: postgres:17.4
    restart: always 
    environment:
      POSTGRES_PASSWORD: postgres
      POSTGRES_USER : postgres
      POSTGRES_DB : postgres
    ports:
      - 5431:5432  
    volumes:
      - db_data:/var/lib/postgresql/data 
volumes:
  db_data:

      